using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Money : Value
{
    // Start is called before the first frame update
    public Money(int defaultValue)
    {
        value = defaultValue;
    }
}
